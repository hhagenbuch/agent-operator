# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: promptversions.agents.hhagenbuch.io
spec:
  group: agents.hhagenbuch.io
  names:
    kind: PromptVersion
    plural: promptversions
    shortNames:
    - pv
    singular: promptversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: "Observed eval pass rate (e.g. \"0.94\"), set once the gate has\
        \ run."
      jsonPath: .status.evalPassRate
      name: PASSRATE
      priority: 0
      type: string
    - description: Pending → Canary → Evaluating → Promoted | RolledBack.
      jsonPath: .status.phase
      name: PHASE
      priority: 0
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              agentRef:
                type: string
              model:
                type: string
              rollout:
                properties:
                  canaryWeight:
                    type: integer
                  strategy:
                    type: string
                type: object
              systemPrompt:
                type: string
            type: object
          status:
            properties:
              evalPassRate:
                type: string
              evalStartedAt:
                type: string
              message:
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
